{
  "name": "static-me",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run watch-md & vite",
    "watch-md": "chokidar 'src/content/**/*.md' -c 'npm run convert-md'",
    "convert-md": "node convert.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "vite": "^5.4.1"
  }
}
